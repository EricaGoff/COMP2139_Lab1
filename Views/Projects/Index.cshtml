@model IEnumerable<COMP2139_Lab1.Models.Project>

@{
    ViewData["Title"] = "Projects 💖";
    Layout = "_Layout";
}

<div class="text-center mt-5">
    <h1 style="color:#ff4d94;">My Projects 💻</h1>
    <p class="mt-3">Here’s a list of all projects created ✨</p>

    <a asp-action="Create" class="btn btn-light border rounded-pill px-4 mt-3" 
       style="color:#ff4d94; border-color:#ff4d94;">
        <i class="fa-solid fa-plus"></i> Add New Project
    </a>
</div>

@if (!Model.Any())
{
    <p class="text-center mt-4" style="color:#ff4d94;">No projects yet! Create one above 💕</p>
}
else
{
    <table class="table table-striped mt-4">
        <thead style="color:#ff4d94;">
        <tr>
            <th>Name</th>
            <th>Owner</th>
            <th>Start Date</th>
            <th>End Date</th>
            <th>Actions</th> <!-- Changed from empty to "Actions" -->
        </tr>
        </thead>
        <tbody>
        @foreach (var project in Model)
        {
            <tr>
                <td>@project.Name</td>
                <td>@project.Owner</td>
                <td>@project.StartDate.ToShortDateString()</td>
                <td>@project.EndDate.ToShortDateString()</td>
                <td>
                    <a asp-action="Details" asp-route-id="@project.Id" 
                       class="btn btn-sm btn-light border rounded-pill me-1" 
                       style="color:#ff4d94; border-color:#ff4d94;">
                        <i class="fa-solid fa-eye"></i> View
                    </a>

                    <a asp-action="Edit" asp-route-id="@project.Id" 
                       class="btn btn-sm btn-warning border rounded-pill me-1" 
                       style="color:white; background-color:#ff4d94; border-color:#ff4d94;">
                        <i class="fa-solid fa-pen"></i> Edit
                    </a>

                    <a asp-action="Delete" asp-route-id="@project.Id" 
                       class="btn btn-sm btn-danger border rounded-pill" 
                       style="color:white; background-color:#ff4d94; border-color:#ff4d94;">
                        <i class="fa-solid fa-trash"></i> Delete
                    </a>
                </td>
            </tr>
        }
        </tbody>
    </table>
}